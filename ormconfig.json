{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "prayush",
  "password": "postgres",
  "logging": true,
  "database": "nepventmanagement",
  "entities": [
    "**/*.entity{.ts,.js}"
  ],
  "synchronize": false,
  "migrationsTableName": "custom_migration_table",
  "migrations": ["migration/*.ts"],
  "seeds": ["seeds/*{.ts,.js}"],
  "cli": {
    "migrationsDir": "migration"
  }
}
